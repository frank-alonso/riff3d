"use client";

/**
 * Editor page -- placeholder until Task 2 adds the full EditorShell.
 * Uses dynamic import to prevent SSR of 3D engine components.
 */
export default function EditorPage() {
  return (
    <div className="flex h-full items-center justify-center text-[var(--muted-foreground)]">
      <p>Loading editor...</p>
    </div>
  );
}
